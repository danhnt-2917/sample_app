<li>
  <%= gravatar_for user, size: Settings.page.size_avatar %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    <%= button_to t(".delete"), user, method: :delete,
      data: { confirm: t(".confirm") } %>
  <% end %>
</li>
