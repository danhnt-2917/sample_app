<% provide :title, t(".title") %>
<% provide :button_text, t(".button_edit") %>
<h1><%= t(".h1") %></h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= render "form" %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <%= link_to t(".change"), Settings.url.gravatar_link, target: "blank" %>
    </div>
  </div>
</div>
